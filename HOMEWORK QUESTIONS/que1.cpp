#include<iostream>
using namespace std;

int main ()
{
    int a , b ;
    cout << "Enter the value of a: ";
    cin >> a; // input the value of a from the user

    cout << "Enter the value of b: ";
    cin >> b; // input the value of b from the user

    cout << "Sum of a and b is : " << (a + b) << endl; // output the sum of a and b

    return 0;
}